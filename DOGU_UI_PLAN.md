# dogu.gg 디자인 통일 작업

## 배경

dogu.gg 아래에 게임별 전적검색 사이트가 붙어 있는 구조다. 각 사이트가 별도 폴더 / 별도 GitHub 저장소 / 별도 Railway 프로젝트로 따로 만들어져서, 헤더 구조·로고·검색창·색이 전부 제각각이다. 이걸 하나의 규격으로 맞춘다.

| 경로 | 폴더 | 비고 |
|---|---|---|
| dogu.gg (랜딩) | 랜딩 폴더 (Cloudflare Pages) | 각 사이트로 가는 관문 |
| dogu.gg/lol | pixlol.kr 저장소 | **현재 pixlol.kr로 리다이렉트 중** |
| dogu.gg/er | dogu_er | **이번 통일의 기준 사이트** |
| dogu.gg/maple | dogu_maple | |
| dogu.gg/loa | dogu_loa | |
| dogu.gg/tft | dogu_tft | |

전 사이트가 바닐라 JS + Express + `public/` 정적 파일 구조다. 빌드 단계 없음.

---

## 작업을 2단계로 나눈다

### 1단계 — 공통 파일 제작 (이 세션에서 먼저)

`dogu_er`의 실제 코드에서 수치를 **읽어서** 공통 파일 3개를 만든다. 눈대중이나 추측으로 값을 쓰지 말고, er의 CSS에서 실제 픽셀값·색상값·투명도를 그대로 뽑아낼 것.

만들 파일:

1. `dogu-ui.css` — CSS 변수(색·간격·폰트) + 헤더 / 검색창 / 푸터 / 404 / 준비중 페이지 스타일
2. `dogu-header.js` — 헤더 마크업 생성 + 게임 스위처 + 검색창 포커스 드롭다운 동작
3. `DOGU_UI.md` — 위 두 파일의 사양서. 각 사이트가 무엇을 커스터마이즈할 수 있고 무엇을 건드리면 안 되는지 명시

두 파일 맨 위에 다음 주석을 넣는다:

```
/* 공통 UI 파일. 이 파일은 원본에서 복사되어 배포된다.
   각 사이트 폴더에서 직접 수정하지 말 것 — 원본을 고치고 다시 복사할 것. */
```

### 2단계 — 각 사이트에 적용 (사이트별 세션에서)

각 폴더에서 하는 일은 **디자인을 결정하는 게 아니라, 1단계 파일을 적용하고 충돌하는 기존 스타일을 걷어내는 것**이다. 판단하지 말고 사양서를 따를 것.

1. `dogu-ui.css` / `dogu-header.js`를 `public/`에 복사
2. `index.html`에서 두 파일을 로드 (`dogu-ui.css` 는 사이트 CSS **앞에**, `dogu-header.js` 는 앱 JS **앞에**). `<body class="dogu-body">`
3. 기존 헤더 / 검색창 / 푸터 / 404 / 준비중 관련 CSS·마크업을 제거. 배경 이미지·오버레이 CSS 도 제거하고 `--dogu-bg-image` 변수로 넘긴다
4. **사이트 라우터에 `DoguUI.setHome()` 과 `DoguUI.setActiveNav()` 를 연결한다 (필수).** 라우터가 사이트마다 달라 복사만으로는 안 된다. 안 하면 홈/비홈 오버레이 농도가 안 바뀌고 네비 활성 표시가 안 붙는다. er 예시는 `DOGU_UI.md` 3절
5. **기존 CSS 와 충돌 확인** — `DOGU_UI.md` 11절 체크리스트대로. 특히 `a { color }` 류의 전역 규칙이 `.dogu-*` 를 덮어쓰는지
6. 아래 "사이트별 개별 지시" 수행
7. 브라우저로 실제 열어서 확인 (홈 ↔ 다른 페이지 이동 때 배경 농도가 바뀌는지 포함)

---

## 공통 규격

### 색

배경색은 사이트별로 그대로 유지한다. 액센트 색만 CSS 변수로 각 사이트가 지정한다.

검색 버튼이 그라데이션(`linear-gradient(135deg, 밝은쪽, 어두운쪽)`)이라 액센트를 **2색**으로 정의한다. 밝은쪽(`--dogu-accent`)이 로고 `.GG`·링크·활성 표시 등 단색 자리에 쓰이고, 어두운쪽(`--dogu-accent-dark`)은 그라데이션 끝에만 쓰인다.

| 사이트 | 배경 (`--dogu-bg`) | 액센트 밝은쪽 (`--dogu-accent`) | 액센트 어두운쪽 (`--dogu-accent-dark`) |
|---|---|---|---|
| 랜딩 | `#17181c` | `#e8a33d` | `#b8741a` |
| lol (pixlol) | `#16112a` | `#9b6be8` | `#6a3fc4` |
| er | `#0a1224` | `#45b3f2` (er 실제 CSS `--accent`) | `#1f6fd0` (er 실제 버튼 그라데이션 끝) |
| maple | `#0d1230` | `#f5c518` | `#c9960a` |
| loa | `#070c1c` | `#6c8cf5` | `#3d5fd4` |
| tft | 기존 값 유지 | 미정 — 일단 er 액센트를 임시로 씀 | 미정 — 일단 er 값을 임시로 씀 |

er 외 사이트의 어두운쪽은 기존 액센트와 같은 계열로 어둡게 잡은 값이다. 필요하면 사이트별 세션에서 바꿔도 되지만, 변수 한 곳에서만 바꿀 것.

`<meta name="theme-color">` 값은 각 사이트의 `--dogu-bg`와 일치시킨다. 지금은 어긋나 있다.

### 로고

두 군데에 나온다. 둘 다 아래 규격으로 통일한다. **둘 다 현재 사이트 홈으로 가는 링크**이고(pixlol 은 pixlol.kr), 커서는 pointer, 텍스트 선택은 안 되게, 밑줄·색 변화 없이 모양 그대로.

- **이탤릭**, 아이콘 없이 텍스트만
- `DOGU` (기본 텍스트색) + `.GG` (액센트색) 두 조각 구조
- pixlol만 예외: `PIXLOL` + `.KR`
- **loa 헤더에 붙어 있는 삼각형 아이콘은 제거한다**
- 랜딩의 시바견 아이콘도 제거한다 (시바견은 각 사이트 파비콘으로 이미 쓰고 있어 중복)

배치:
1. 헤더 좌상단 — 작은 크기, 클릭 시 해당 사이트 홈으로
2. 히어로 중앙 검색창 위 — 큰 크기

### 헤더 — 2단 구조

er의 헤더가 기준이다. **반투명 + backdrop blur**로 히어로 이미지가 뒤로 비쳐야 한다. 높이·패딩·투명도·blur 값은 er CSS에서 그대로 뽑아 쓸 것.

**1단**: 로고 (좌) / 게임 스위처 (로고 우측) / 검색창 (우) — 1단 검색창은 **홈 포함 모든 화면에서 상시 노출**이고, 768px 이하에서만 숨긴다. er 현재 동작 그대로다. 스크롤에 따라 나타나고 사라지는 동작은 er 에 없으므로 **새로 만들지 않는다**.

**2단**: 네비게이션 탭 (좌) / 보조 정보 (우, 예: 패치 버전)

현재 loa와 pixlol은 1단 헤더다. 2단으로 바꾼다. maple은 2단이지만 1단에 검색창이 없다. 추가한다.

### 게임 스위처

전 사이트 헤더 1단에 넣는다. 현재 er에만 있고, 심지어 목록이 실제 사이트 구성과 안 맞는다(발로란트·배틀그라운드가 들어 있음). 아래 순서로 교체한다.

1. 리그 오브 레전드 → `https://pixlol.kr` (외부 이동. dogu.gg/lol이 아니라 pixlol.kr로 직접)
2. 이터널 리턴 → `/er`
3. 메이플스토리 → `/maple`
4. 로스트아크 → `/loa`
5. 전략적 팀 전투(TFT) → `/tft`

현재 보고 있는 사이트는 스위처에서 활성 표시한다. 랜딩(dogu.gg)에는 스위처를 넣지 않는다 — 랜딩 자체가 스위처 역할이다.

### 검색창

er 규격으로 통일한다.

- **알약 모양** 입력창(`border-radius: 999px`, `overflow: hidden`) + 우측 버튼. 버튼은 별도 radius 없이 박스의 알약 모양에 잘려 들어간다
- **버튼 안 내용은 사이트별 설정값이다.** dogu.gg 계열(er·maple·loa·tft)은 `.GG` 글자, **pixlol 만 돋보기 아이콘**. 글자가 검색 버튼으로 안 읽힐 수 있으니 버튼에 `aria-label="검색"` 을 넣는다
- 버튼 색은 각 사이트 액센트 2색 그라데이션(`135deg`, 밝은쪽 → 어두운쪽)
- **월드/서버 선택 UI 없음** (maple 길드 검색은 예외 — 아래 참조)
- **검색창 안에 셀렉트를 두지 않는다.** er 의 `.search-queue`(큐 셀렉트)는 현재 화면에 안 쓰이는 잔재라 공통 규격에 포함하지 않는다. er 쪽 잔재는 이번 작업에서 건드리지 않고 그대로 둔다

**즐겨찾기 / 최근 검색은 검색창을 클릭(포커스)했을 때만 드롭다운으로 표시한다.** 현재는 셋 다 상시 노출이다. 포커스가 빠지면 닫힌다.

용어와 문구를 아래로 통일한다. 현재 "최근 검색 / 최근검색 / 최근기록" 세 가지로 갈려 있다.

- 라벨: `★ 즐겨찾기` / `🕘 최근 검색`
- 안내문: `각각 10개까지 저장됩니다.`
- 빈 상태: `저장된 항목이 없습니다.`

### 배경 구조

er 구조를 전 사이트 규격으로 삼는다. **히어로 안에만 이미지를 넣는 방식은 쓰지 않는다.**

- 배경 이미지는 `body` 에 `background: url(...) no-repeat center center fixed` + `background-size: cover` 로 깐다 (`background-attachment: fixed`)
- 오버레이는 `body::before` 가 페이지 전체를 덮는다 (`position: fixed; top: -150px; bottom: -150px; z-index: -1`). 히어로 전용 오버레이가 아니다
- **`html` 에 배경(색 포함)을 깔면 안 된다.** body 배경의 루트 캔버스 승격이 멈춰 오버레이가 이미지 밑으로 들어간다 (pixlol 에서 실제로 밟은 사고)
- **홈과 그 외 페이지의 오버레이 농도를 다르게 둔다** (er 방식). 홈은 옅게(히어로 구간 `0.52 → 0.56 @520px → 0.9 @880px → 0.93`), 그 외 페이지는 진하게(`0.8 → 0.93 @560px → 0.93`). px 값은 오버레이가 위로 150px 삐져나가 있어 뷰포트 기준 +150 이다
- **오버레이 농도는 사이트별 CSS 변수로 조절할 수 있게 한다.** 기본값은 위 er 값이고, 밝은 배경 이미지(메이플 하늘)는 더 진하게 쓸 수 있어야 한다
- **배경 이미지 블러는 공통 변수 하나로 두되 기본값은 0** 이다. er 이 블러 없이 오버레이만으로 가독성을 확보하고 있으므로 기본은 블러 없음. 현재 maple 과 pixlol 은 블러가 걸려 있다 — 걷어낸다. loa 는 이미 블러 없는 어두운 이미지라 그대로 두면 된다

### 히어로

- 히어로에 높이를 지정하지 않는다. **패딩 값을 전 사이트 동일하게 통일한다 (er 기준 `74px 16px 62px`, 768px 이하 `32px 16px 28px`).** 현재 pixlol 이 유독 긴 것은 이 패딩으로 맞추면서 해소한다
- 히어로에 별도 판·경계선을 깔지 않는다. 배경 오버레이의 옅은 구간이 히어로 자리다

### 다크 모드 고정

- **라이트 모드를 없앤다.** maple 헤더의 달/해 토글 버튼을 제거하고, 관련 라이트 모드 CSS와 localStorage 테마 저장 로직도 함께 제거한다.
- 다른 사이트에도 테마 토글을 추가하지 않는다.

### 껍데기 UI 제거

- maple 헤더의 `로그인` 버튼 — 실제 기능이 없다. 제거한다.
- er 헤더의 🌐 아이콘 — 실제 기능이 없다. 제거한다.

### 푸터 / 404 / 준비중 페이지

현재 사이트마다 구성과 문구가 다르다. 아래로 통일한다.

푸터 구성:
1. 로고
2. 링크: `이용약관` | `개인정보 처리방침` | `버그제보 및 피드백`
3. 게임사 권리 고지 (사이트별 문구는 각자 유지)
4. `Contact: 00.y4no@gmail.com`

404 문구: 제목 `404`, 본문 `요청하신 페이지를 찾을 수 없습니다.`, 링크 `← 홈으로 돌아가기`

준비중 문구: 제목 `준비 중`, 본문 `아직 만드는 중인 페이지입니다. 조금만 기다려 주세요.`, 링크 `← 홈으로 돌아가기`

### 페이지 타이틀

`<title>`과 `og:site_name` 포맷을 통일한다. 현재 `도구지지 - 이터널리턴`, `DOGU.GG: 메이플스토리`, `도구지지 - 로스트아크`로 셋 다 다르다.

- 형식: `DOGU.GG: {게임명}` (예: `DOGU.GG: 이터널리턴`, `DOGU.GG: 메이플스토리`)
- pixlol만: `PIXLOL.KR` 그대로
- **사이트 이름은 형식만 규격이고, 어디에 적는지는 사이트마다 다르다.** er 은 `.env` 의 `SITE_NAME`, pixlol 은 `.env` 에 그 항목이 없어 코드에 박혀 있을 것이다. 각 사이트에서 자기 방식대로 적용할 것

---

## 사이트별 개별 지시

### dogu.gg (랜딩)

- 배경이 회색 `#17181c`인데 하위 사이트는 전부 남색/보라 계열이라, 관문에서 들어갈 때 색이 튄다. 하위 사이트와 어울리는 톤으로 조정한다.
- 히어로 설명문에 TFT가 빠져 있다 (`리그 오브 레전드 · 이터널 리턴 · 메이플스토리 · 로스트아크`). meta-description에는 TFT가 들어 있어 서로 안 맞는다. TFT를 추가한다.
- TFT 카드에 `준비 중` 뱃지가 붙어 있는데, dogu.gg/tft는 이미 배포되어 동작 중이다. 실제로 열어서 확인하고, 정상 동작하면 뱃지를 뗀다.
- 게임 카드 순서는 지금 그대로(롤-이터널리턴-메이플-로아-TFT) 두면 된다.

### dogu.gg/lol (pixlol.kr)

- **헤더와 히어로 로고에 `DOGU.GG`가 아니라 `PIXLOL.KR`을 쓴다.** 라이엇 API 심사 때문에 현재 dogu.gg/lol이 pixlol.kr로 리다이렉트되고 있어서다.
- 검색 버튼은 **돋보기 아이콘을 유지한다.** 다른 사이트는 `.GG` 글자를 쓰지만 도메인이 pixlol.kr이라 `.GG`가 맞지 않는다. 공통 파일의 `search.button` 에 돋보기를 지정한다.
- 헤더·히어로 로고 링크는 pixlol.kr 홈으로 간다.
- 배경을 body + `body::before` 구조로 옮기고 블러를 걷어낸다.
- 히어로 패딩을 공통 규격(74px / 62px)으로 맞춰 길이를 줄인다.
- 1단 헤더를 2단으로 바꾼다. 기존 탭 6개(전적검색/랭킹/통계/챔피언/도감/신화상점)는 2단으로 내린다.
- **주의**: 이 저장소에는 추적 중인 백업 파일들이 있다. 커밋할 때 `git add -A`를 쓰지 말고 변경 파일을 명시적으로 지정할 것. `riot.txt`와 `.env`는 절대 삭제하지 말 것.

### dogu.gg/er

- **2026-08-22 적용 완료** (아래 항목 전부 + 2단계 절차). 라우터 연결은 `router.js` 의 `render()`, 네비 항목은 `routes.js` 의 `navItems()`
- 기준 사이트지만, 여기서도 바꿀 게 있다.
- 게임 스위처 목록을 위의 5개로 교체한다 (현재 발로란트·배틀그라운드가 들어 있고 TFT·메이플·로아가 빠져 있다).
- 헤더 🌐 아이콘을 제거한다.
- 즐겨찾기/최근 검색을 상시 노출에서 포커스 드롭다운으로 바꾼다.

### dogu.gg/maple

- **메인 캐릭터 검색창의 `전체 월드` 셀렉트를 제거한다.** 월드 지정 없이 검색되게 한다.
- **단, 길드 검색의 월드 선택은 그대로 둔다.** 길드는 월드마다 같은 이름이 존재할 수 있어 구조상 월드가 필수다. 사이트 안내문에도 그렇게 적혀 있다.
- 검색창의 `/ 키를 눌러 검색` 힌트는 유지해도 되지만, 다른 사이트에도 같은 단축키를 넣어 동작을 통일할 것.
- 검색 버튼이 노란 원형이다. 공통 규격(알약 검색창에 붙은 액센트 그라데이션 버튼)으로 바꾼다.
- 헤더 달/해 토글과 `로그인` 버튼을 제거한다.
- 배경을 body + `body::before` 구조로 옮기고 블러를 걷어낸다. 하늘 배경이 밝으니 오버레이 농도 변수를 er 보다 진하게 잡는다.
- 로고를 정자체에서 이탤릭으로 바꾼다.

### dogu.gg/loa

- **메인 검색창의 서버 셀렉트(전체/루페온/실리안/…)를 제거한다.** 서버 지정 없이 검색되게 한다.
- 단, 랭킹 표에 있는 `서버` 컬럼과 랭킹 필터링은 그대로 둔다.
- 검색창 테두리가 파란 실선이라 유독 튄다. 공통 규격으로 바꾼다.
- 검색 버튼이 각진 사각이다. 공통 규격의 알약 모양으로 바꾼다.
- 헤더 좌측 삼각형 아이콘을 제거한다.
- 1단 헤더를 2단으로 바꾼다.
- 로고를 정자체에서 이탤릭으로 바꾼다.
- 히어로 배경 이미지는 지금 상태(블러 없는 어두운 이미지)가 규격에 맞다. 이미지 자체는 건드리지 말고, 배치만 body + `body::before` 구조로 옮긴다.

### dogu.gg/tft

- 다른 사이트와 같은 규격을 적용한다.
- 액센트색이 아직 안 정해졌다. 일단 er 액센트를 임시로 쓰고, 나중에 교체할 수 있게 CSS 변수 한 곳에서만 관리한다.
- 작업 전에 브라우저로 dogu.gg/tft를 열어서, `/tft` prefix 기준으로 정적 리소스(JS/CSS)가 정상 로드되는지 먼저 확인한다. 깨져 있으면 그것부터 고치고 진행한다.

---

## 하지 말 것

- **데이터 로직을 건드리지 마라.** 이번 작업은 순수 UI 통일이다. API 호출, DB 쿼리, 캐싱, 랭킹 집계, 파싱 로직은 손대지 않는다.
- 공통 파일을 각 사이트 폴더에서 개별 수정하지 마라. 특정 사이트만의 요구가 생기면 CSS 변수로 노출하거나, 사양서에 예외로 명시하고 원본을 고쳐서 다시 복사한다.
- 디자인을 스스로 판단해서 개선하지 마라. 사양이 애매하면 물어봐라.
- 커밋 전에 각 사이트를 실제 브라우저로 열어서 확인해라. 특히 헤더 반투명, 스위처 이동, 검색창 포커스 드롭다운, 홈/비홈 오버레이 농도 차이 네 가지는 눈으로 봐야 안다.
